import React from 'react';
import SignInButton from '../Auth/SignInButton';

const HomePage = () => (
  <div class="container">
    <div class="row">
      <SignInButton />
    </div>
  </div>
);

export default HomePage;
